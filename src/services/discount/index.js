const discountHandlerCallback = require('./callback');
const promiseHandler = require('./promise');
const asyncHandler = require('./async');

module.exports = {
  discountHandlerCallback,
  promiseHandler,
  asyncHandler,
};
